apiVersion: v1
kind: Service
metadata:
  name: backend-sql
  namespace: inventario
spec:
  selector:
    app: backend-sql
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30001
  type: NodePort
