apiVersion: v1
kind: Service
metadata:
  name: inventario-web
  namespace: inventario
spec:
  selector:
    app: inventario-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort

