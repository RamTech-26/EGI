apiVersion: v1
kind: Service
metadata:
  name: backend-mongo
  namespace: inventario
spec:
  selector:
    app: backend-mongo
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
